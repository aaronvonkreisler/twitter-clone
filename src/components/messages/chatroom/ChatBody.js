import React from 'react';
import PropTypes from 'prop-types';
import '../../../styles/design/messageDisplay.css';

const ChatBody = (props) => {
   return (
      <div className="chat-body" style={{ color: 'white', fontSize: '20px' }}>
         Chat Body Area
      </div>
   );
};

ChatBody.propTypes = {};

export default ChatBody;
